<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Opcional y bajo demanda (solo botón "Mejorar diagnóstico"): distingue datos
         apagados de sin cobertura. Nunca se pide en onboarding ni bloquea la jornada. -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

        <!-- 1.1.8: visibilidad de paquetes OEM para abrir sus pantallas de
         autostart/batería (Android 11+ exige declararlas). Los intents se
         lanzan con try/catch y fallback a Ajustes de la app. -->
    <queries>
        <package android:name="com.hihonor.systemmanager" />
        <package android:name="com.huawei.systemmanager" />
        <package android:name="com.transsion.phonemaster" />
        <package android:name="com.transsion.phonemanager" />
        <package android:name="com.transsion.batterylab" />
        <package android:name="com.mediatek.autobootcontroller" />
        <package android:name="com.zte.powersavemode" />
        <package android:name="com.miui.securitycenter" />
        <package android:name="com.miui.powerkeeper" />
        <package android:name="com.samsung.android.lool" />
    </queries>

    <application
        android:name=".DmujeresApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dmujeres"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.MainActivity"
            android:exported="false"
            android:launchMode="singleTask" />

        <activity
            android:name=".ui.OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.DiagnosticsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.ActivationGuideActivity"
            android:exported="false" />

        <!-- TEMPORAL debug de diseño: borrar al pasar a producción. -->
        <activity
            android:name=".ui.DebugDesignActivity"
            android:exported="false" />

        <!-- R9: transición de geofence de arranque (canal exento por el OS). -->
        <receiver
            android:name=".recovery.GeofenceWakeReceiver"
            android:exported="false" />

        <!-- F2: receptor del probe de recovery FCM (data message HIGH priority). -->
        <service
            android:name=".recovery.FcmRecoveryMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Sentry: SIN auto-init (sus providers se removían/iniciaban antes que la
             app y pueden tumbar el arranque). Solo inicia manual en DmujeresApp,
             protegido con try/catch. -->
        <provider
            android:name="io.sentry.android.core.SentryInitProvider"
            tools:node="remove" />
        <provider
            android:name="io.sentry.android.core.SentryPerformanceProvider"
            tools:node="remove" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.dmujeres.traccar.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".tracking.TrackingService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="location" />

        <!-- Requerido por WorkManager expedited (TrackingRecoveryWorker): tipo
             foreground declarado o el lint vital bloquea la release. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="location"
            tools:node="merge" />

        <receiver
            android:name=".recovery.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
                <!-- Arranque rápido en HTC/algunas ROMs (no existe en Intent). -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Guardián de sesión (alarma cada 15 min con jornada abierta):
             canal anti-OEM para revivir el servicio si el fabricante mata
             el FGS y congela el worker. exported=false, solo nuestras alarms. -->
        <receiver
            android:name=".recovery.SessionKeeperReceiver"
            android:exported="false" />

        <!-- Widget de jornada en pantalla de inicio (RemoteViews puro: compatible
             con cualquier launcher/OEM). Solo ACTION_APPWIDGET_UPDATE: el refresco
             fino lo empuja TrackingService con JourneyWidget.updateAll(). -->
        <receiver
            android:name=".ui.widget.JourneyWidget"
            android:exported="true"
            android:label="@string/widget_label"
            android:description="@string/widget_desc">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/journey_widget_info" />
        </receiver>

    </application>

</manifest>
