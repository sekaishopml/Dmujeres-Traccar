<?xml version="1.0" encoding="utf-8"?>
<!--
  Widget de jornada 2x1 (RemoteViews puro = máxima compatibilidad de launchers).
  Horizontal: icono + estado grande y subtítulo a la izquierda (peso 1), botón
  píldora a la derecha con tamaño fijo. Todo centrado verticalmente para que
  quepa en una celda de altura sin comprimirse.
  Solo vistas soportadas por RemoteViews: LinearLayout/TextView/ImageView.
  El fondo del botón se cambia por código (verde/rojo).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:minHeight="60dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/widget_background"
    android:paddingStart="10dp"
    android:paddingEnd="10dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@mipmap/ic_launcher"
        android:contentDescription="@string/widget_label" />

    <LinearLayout
        android:id="@+id/widget_content"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/widget_status"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:textColor="#181818"
            android:textSize="17sp"
            android:textStyle="bold"
            android:text="@string/widget_idle" />

        <TextView
            android:id="@+id/widget_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="3dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:includeFontPadding="false"
            android:textColor="#666666"
            android:textSize="10sp"
            android:text="@string/widget_waiting" />
    </LinearLayout>

    <TextView
        android:id="@+id/widget_action"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="78dp"
        android:minHeight="36dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/widget_button_verde"
        android:gravity="center"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:maxLines="1"
        android:includeFontPadding="false"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:textStyle="bold"
        android:text="@string/action_start" />

</LinearLayout>
