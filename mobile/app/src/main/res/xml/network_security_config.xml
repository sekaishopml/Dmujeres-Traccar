<?xml version="1.0" encoding="utf-8"?>
<!--
  S2: por defecto SOLO TLS. Excepción operativa documentada: el servidor
  DMujeres desplegado hoy sirve HTTP en :999 (OTA + fallback de posiciones),
  así que se permite cleartext únicamente a esos hosts conocidos.
  - Un host nuevo exige TLS (recomendado) o un rebuild con lista actualizada.
  - MQTT (tcp://:1883) no pasa por esta política; su TLS es una tarea de
    infraestructura separada (ver docs/SECURITY.md).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Servidor de producción y nodo Tailscale de operación. -->
        <domain includeSubdomains="false">68.168.20.219</domain>
        <domain includeSubdomains="false">100.83.91.53</domain>
        <domain includeSubdomains="false">100.86.171.63</domain>
        <!-- Desarrollo local (emulador/host). -->
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
