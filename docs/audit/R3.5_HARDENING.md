# R3.5_HARDENING.md — Production Hardening antes de R4 (2026-09-17)

> Implementación del hardening R3.5. Sin commits (esperando autorización).
> Veredicto al final del documento. Nunca "production ready" por compilar.

## 1) Implementado en esta fase

### 1.1 OTA: integridad verificable end-to-end (R3.5-14, hallazgo ALTO agente E)

| Paso | Estado | Evidencia |
|---|---|---|
| CHECK/DOWNLOAD | existente | `platform/UpdateManager.kt` (2 intentos + tamaño mínimo 5 MB) |
| **SHA256_VERIFY** | **NUEVO** | `apkSha256Matches(file, expected)` puro JVM; `downloadOnce` rechaza y BORRA el APK si el checksum difiere (`lastError="checksum SHA-256 no coincide"`); `latest.sha256` viaja desde `latest.json`/asset `digest` de GitHub; MainActivity pasa `latest.sha256` |
| Servidor | **NUEVO** | `publish-ota.sh` escribe `sha256` en `latest.json` (ambos dirs); publicado y verificado: `sha256=ac9cf7d3…` == sha del APK servido (`/tmp/opencode/served.apk`) |
| POST_INSTALL / MIGRATION / HEALTH verify | parcial | SessionKeeper post-install + readiness post-OTA ya existentes; verificación programática completa = R4 |
| Compatibilidad | OK | `sha256` null/blank = sin verificación (latest.json viejo sigue funcionando); GitHub `digest` parseado y testeado |
| Tests | 592/0 | `UpdateManagerTest`: digest en release, match/upper-case, determinismo/sensibilidad de contenido, sin expectativa = instala |

### 1.2 Room migrations (R3.5-15)

- **NUEVO** `androidTest/.../HealthSnapshotMigration89Test.kt`: crea BD v8 exacta
  (health_snapshots sin `uploadedAt`/`healthState`), abre con Room+MIGRATION_8_9,
  valida: datos sobreviven (pending + snapshot), defaults honestos
  (`uploadedAt=0`, `healthState='UNKNOWN'`), runtime con los campos nuevos.
- **FIX** `HealthSnapshotMigrationTest` (7→8): registraba solo 7→8 contra DB
  destino v9 → Room habría fallado al buscar 8→9; ahora registra ambas.
- Compilación androidTest: **OK**. **Ejecución pendiente de dispositivo** (ZTE
  offline; se ejecuta en la fase física R4).

### 2) Otros hardening verificados (sin código nuevo necesario)

- **Replay**: no unir CAPTURE_GAP — B-1 cerrado (conectores respetan `shouldCut`)
  + `routeSegmentation.js` con matriz A–G (12/12). Raw intacto (MUST-NOT test).
- **Sensor fusion**: cero coordenadas sintéticas — `LocationSensorFusion` "NO
  produce lat/lon" + tests de readquisición sin inventar posición.
- **FCM**: el cliente NO puede auto-declarar SUCCESS — validación server-side
  (F2, `FcmRecoveryService` con 6 etapas de evidencia; `FcmSenderTest`,
  `FcmRecoveryPolicyTest`).
- **Recovery ladder**: estados/límites en políticas puras con tests
  (`RecoveryOutcome`, `RecoveryJournal`, `SessionKeeper`).
- **Seguridad**: gating de firma con fallo duro (R2, probado); backups con
  secretos restringidos 0600; NSC restrictiva; R8; sin JSON de servicio en APK.
- **Dashboard**: 92/92; visual fiel (no unir huecos; DELIVERY_DELAY indicable).

## 2) TABLA DE GATES (13) antes de R4

| # | Gate | Estado | Evidencia |
|---|---|---|---|
| 1 | Unit/domain tests | **PASS** | móvil 592/0 (588 + 4 checksum) |
| 2 | Android compile | **PASS** | `:app:compileDebugKotlin` OK |
| 3 | Lint | **PASS** | `:app:lintDebug` OK |
| 4 | Room migration androidTest | **COMPILA · EJECUCIÓN PENDIENTE** (device) | 8→9 nuevo + fix 7→8; `compileDebugAndroidTestKotlin` OK |
| 5 | Outbox offline/restart/replay/duplicate | **PASS** (unit) | `DispatchRobustnessTest`, `PositionOutboxMicroBatchTest`, `OfflineRouteOrderTest` |
| 6 | Location timeout/fallback | **PASS** (unit) | `LocationTimeoutController*`, `LocationEnginePolicy*` |
| 7 | Sensor fusion sin sintéticos | **PASS** | `LocationSensorFusionTest` ("exige readquisición sin inventar posición") |
| 8 | Recovery ladder transiciones | **PASS** (unit) | `Recovery*PolicyTest` (transiciones/limits) |
| 9 | FCM no auto-declara SUCCESS | **PASS** (unit, server ya lo validó en F2) | `FcmSenderTest` + regla server-side |
| 10 | Replay no une CAPTURE_GAP | **PASS** | B-1 test + `routeSegmentation` (cut ⇔ tipo) |
| 11 | OTA checksum+migration+preservación | **PASS** (unit+E2E server) · migration = gate 4 | sha256 publicado y verificado; preservación Room = androidTest (gate 4) |
| 12 | Security scan | **PASS** (ronda) | `docs/audit/SECURITY_AUDIT.md`; sin commits de secretos; backups 0600 |
| 13 | Dashboard build/tests | **PASS** | 92/0 + build publicado (B-1/B-2/B-4) |

## 3) Pendiente explícito (no bloquea READY con condiciones)

1. **Ejecutar** androidTest 8→9 y 7→8 en dispositivo (R4).
2. Wake lock parcial + alarma exacta (F12, auditor B P0): implementación difiere
   hasta poder MEDIR batería en dispositivo (regla 11: sin evidencia no se
   aumenta consumo) — spec en `docs/audit/OEM_BACKGROUND_AUDIT.md`.
3. Rotación de secretos coordinada (API key cuando la flota esté ≥1.1.5;
   `release.keystore` regenerada antes de migrar firma).
4. TLS/HTTPS (infra) + tokens por dispositivo.

## 4) Veredicto

**READY_FOR_R4** — con las condiciones documentadas arriba (androidTest 8→9
PENDIENTE de ejecución en dispositivo y rotaciones coordinadas). Pruebas físicas
6/12/24 h: **PENDING REAL-WORLD VALIDATION** (Fase R4, no ejecutada).
