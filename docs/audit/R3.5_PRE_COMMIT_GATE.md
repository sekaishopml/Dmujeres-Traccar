# R3.5_PRE_COMMIT_GATE.md — Gate final antes de commits (2026-09-17)

## 1) ESTADO

**R3.5 HARDENING VERIFIED WITH BLOCKED ENVIRONMENT GATE — READY_FOR_R4-CONDITIONAL**

Único gate no ejecutado: androidTests de migración Room (8→9 y 7→8) por FALTA DE
DISPOSITIVO/EMULADOR en este entorno. Todo lo demás fue EJECUTADO hoy con
comandos reales. No se ejecuta la campaña física R4.

## 2) GATES (ejecutados en esta ronda)

| GATE | RESULT | EVIDENCE | COMMAND | NOTES |
|---|---|---|---|---|
| 1 Unit tests | PASS (EJECUTADO) | 592/0/0 | `./gradlew :app:testDebugUnitTest` | +4 tests checksum OTA |
| 2 Compile (debug+release) | PASS (EJECUTADO) | BUILD SUCCESSFUL 23 s | `:app:compileDebugKotlin :app:compileReleaseKotlin` | — |
| 3 Lint (debug+release) | PASS (EJECUTADO) | BUILD SUCCESSFUL 35 s | `:app:lintDebug :app:lintRelease` | — |
| 4 Room androidTest | **BLOCKED — ANDROIDTEST DEVICE UNAVAILABLE** | Compilado (`compileDebugAndroidTestKotlin` OK). Ejecución: adb vacío; ZTE tailscale offline 13 h; emulador intentado real: `FATAL | CPU Architecture 'arm' is not supported by the QEMU2 emulator` (sin `/dev/kvm`); `mobile_list_available_devices` timeout ×2 | ver §3 | NO se declara PASS; los 2 tests están listos (nuevo 8→9 + 7→8 corregido para v9) |
| 5 Outbox offline/restart/replay/duplicate | PASS (unit, ejecutado) | dentro de la suite 592/0 | `DispatchRobustnessTest`, `OfflineRouteOrderTest`, `PositionOutboxMicroBatchTest` | instrumented E2E = R4 |
| 6 Location timeout/fallback | PASS (unit, ejecutado) | dentro de la suite | `LocationTimeoutController*`, `LocationEnginePolicy*` | — |
| 7 Sensor fusion sin sintéticos | PASS (unit, ejecutado) | dentro de la suite | `LocationSensorFusionTest` ("exige readquisición sin inventar posición"; clase declara "NO produce lat/lon") | — |
| 8 Recovery ladder | PASS (unit, ejecutado) | dentro de la suite | `Recovery*PolicyTest` (transiciones/límites/journal) | — |
| 9 FCM SUCCESS protection | PASS (unit, ejecutado) | dentro de la suite | `FcmSenderTest`, `FcmRecoveryPolicyTest` + validación server-side (F2, 6 etapas) | E2E real = R4 |
| 10 Replay CAPTURE_GAP | PASS (unit, ejecutado) | 92/92 dashboard + 12/12 segmentación | `node --test src` | B-1 cerrado; cut⇔tipo congelado |
| 11 OTA checksum | PASS (E2E con archivos reales) | esperado `ac9cf7d3…` == real; tamper simulado → sha difiere (rechazado y borrado por `downloadOnce`, verificado en unit) | curl APK + sha256sum + tamper dd; `UpdateManagerTest` | case-insensitive y compat latest.json antiguo = unit (`apkSha256Matches(f,null)`, uppercase) |
| 12 Security scan | PASS (ronda) | sin secretos nuevos trackeados (`git status` solo muestra `*.example`); `mobile/keystore.properties`, `mobile/secrets.properties`, `mobile/app/google-services.json` gitignored; `backups/*.tar.gz` 0600 | `find` + `git status` (§3 del doc) | rotación = READY_FOR_COORDINATED_ROTATION (no ejecutada) |
| 13 Dashboard | PASS (EJECUTADO) | 92/92 | `node --test src` | build publicado con B-1/B-2/B-4; OTA 200 |

## 3) Gate 4 — evidencia del bloqueo de entorno (real, no asumido)

1. `ls /dev/kvm` → no existe (sin aceleración x86_64).
2. `sdkmanager` instaló emulator + imagen ARM (android-10 google_apis
   armeabi-v7a) — descarga real completada.
3. `avdmanager create avd r35test` — AVD creado.
4. Arranque real del emulador:
   `FATAL | CPU Architecture 'arm' is not supported by the QEMU2 emulator,
   (the classic engine is deprecated!)` → sin KVM no hay vía de emulación.
5. `adb devices` (puerto 5038): lista vacía.
6. `mobile_list_available_devices`: timeout ×2.
7. ZTE (qa-f0): `offline, last seen 13h ago` (Tailscale).

Conclusión: **ANDROIDTEST DEVICE UNAVAILABLE** → gate 4 NO declarado PASS.

## 4) OTA FINAL (verificado sin modificar el mecanismo)

- latest.json → sha256 → APK descargado → SHA real: **COINCIDE** (curl +
  sha256sum, arriba).
- APK alterado en 1 byte → sha difiere → el instalador lo rechaza y borra
  (unit: `apkSha256Matches=false`, `downloadOnce` delete + lastError).
- Comparación case-insensitive: unit (`real.uppercase()` → true).
- latest.json antiguo sin sha256: compatibilidad documentada y testeada
  (`apkSha256Matches(f, null)` → true; instalación como antes).

## 5) UPGRADE / DATA PRESERVATION (sin rollout)

**CAMBIO DE FIRMA — determinado con evidencia (no asumido):**

| APK | versionCode | Cert SHA-256 |
|---|---|---|
| DMujeres-Tracking-1.1.2 (flota) | 112 | `867ba129…42f22` |
| DMujeres-Tracking-1.1.5 (actual) | 115 | `867ba129…42f22` |

**1.1.2 → 1.1.5: ACTUALIZACIÓN NORMAL (misma identidad de firma, versionCode
mayor). NO requiere desinstalación/reinstalación. FLEET MIGRATION REQUIRED: NO.**

- Room 112→115: migraciones 8→9 registradas y compilan; ejecución en
  dispositivo pendiente (gate 4) — los datos (outbox/journey/token) viven en
  SharedPreferences+DB que `install -r` preserva.
- Simulación de upgrade en ambiente de prueba: NO EJECUTABLE (sin dispositivo)
  → queda como primera acción de R4 (ZTE: `adb install -r` 1.1.5 sobre la
  instalación existente, luego validar outbox/journey/FCM/TrackingService).

## 6) WAKE LOCK / EXACT ALARM

**DIFERIDO (correcto):** sin evidencia de beneficio (R4) no se aumenta consumo.
Spec lista en `docs/audit/OEM_BACKGROUND_AUDIT.md` (P0) para implementar con
medición en R4.

## 7) SECRET ROTATION — READY_FOR_COORDINATED_ROTATION (no ejecutar aún)

Procedimiento preparado (sin tocar la flota):

| Orden | Acción | Compatibilidad mínima | Rollback |
|---|---|---|---|
| 1 | Flota ≥1.1.5 confirmada (OTA) | apps ≥1.1.5 aceptan clave nueva | mantener `MOBILE_HTTP_API_KEY_PREVIOUS` |
| 2 | Rotar `MOBILE_HTTP_API_KEY` (ventana nueva/anterior en `.env`) + regenerar `mobile/secrets.properties` + build 1.1.6 | ventana acepta actual+anterior | restaurar `.env` previa + restart |
| 3 | Regenerar `release.keystore` (comprometida) ANTES de cualquier migración de firma | solo afecta releases futuros | N/A (key vieja inválida) |
| 4 | DB password (ventana de mantenimiento) | server+compose coordinados | revertir `.env` + restart |
| 5 | EMQX dashboard/consumer + `DASH_ADMIN_PASSWORD` + `WEB_SECRET_TOKEN` | invalida sesiones (documentar) | ídem |
| 6 | TLS 999/1883 + NSC restrictiva | requiere certs (Let's Encrypt) | abrir cleartext temporal |

Validación de tokens: FCM tokens por dispositivo (prefijo hash en logs, nunca
completos). Validación de keystore: `apksigner verify --print-certs` tras cada
build. **No ejecutar sobre la flota hasta autorización.**

## 8) RIESGOS (sin convertir en PASS)

ZTE cfreezer / Doze real / screen-off real / endurance 6-12-24 h / multi-OEM /
FCM E2E real / batería-thermal / force-stop / reboot / secret rotation:
**SIN EVIDENCIA FÍSICA — NO PASS.** Lista viva en `R3.5_HARDENING.md` §3 y
`TECHNICAL_DEBT.md` (listas A/Q).

## 9) Pendientes R4 (resumen)

1. Ejecutar androidTests de migración (8→9 nuevo, 7→8 corregido) en dispositivo.
2. Simulación de upgrade real: instalar 1.1.2 → 1.1.5 (`adb install -r`) y
   validar outbox/journey/token/TrackingService.
3. Wake lock parcial + alarma exacta CON medición de batería.
4. Matriz OEM (ZTE/Honor/Infinix/Samsung/Xiaomi) + endurance 6/12/24 h.
5. FCM E2E real (con token registrado en cada dispositivo).
6. Rotaciones coordinadas (tabla §7 de este doc).

## 10) Condiciones para autorizar commits

1. Revisar y aprobar este doc + `R3_MASTER_AUDIT.md` + `R3_AGENT_REPORT.md`.
2. (Opcional pero recomendado) dispositivos disponibles → cerrar gate 4 y pasar
   a estado A) "HARDENING VERIFIED / READY_FOR_R4".
3. Mensaje(s) de commit aprobados por el operador; solo entonces commitear.

---

R3.5 STATUS:
R3.5 HARDENING VERIFIED WITH BLOCKED ENVIRONMENT GATE — READY_FOR_R4-CONDITIONAL

COMMITS:
NOT AUTHORIZED

R4:
NOT EXECUTED

NEXT ACTION:
Recuperar un dispositivo Android (ZTE qa-f0 en Tailscale o emulador con KVM en otro host), ejecutar `./gradlew :app:connectedDebugAndroidTest --tests "*HealthSnapshotMigration*"` para cerrar el gate 4 con ejecución real, y luego autorizar los commits.
